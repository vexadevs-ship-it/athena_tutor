[project]
name = "athena-agents"
version = "0.1.0"
description = "Athena AI tutoring agents powered by Agno"
requires-python = ">=3.11"
dependencies = [
    "agno",
    "mcp",
    "fastapi",
    "uvicorn[standard]",
    "openai",
    "anthropic",
    "supabase",
    "pydantic",
    "python-dotenv",
    "resend",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
